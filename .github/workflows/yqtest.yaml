name: Create Cluster

on:
  workflow_dispatch:
    
jobs:
  CI:
    runs-on: ubuntu-latest
    
    steps:
    
    - uses: actions/checkout@v1
    
    - name: yq setup
      run: | 
        sudo wget -O /usr/local/bin/yq https://github.com/mikefarah/yq/releases/download/3.3.0/yq_linux_amd64
        sudo chmod +x /usr/local/bin/yq
    
    - name: Replace
      run: |
        cat edit.yaml
        yq w -i -d 1 edit.yaml spec.template.spec.containers.image OUT_NEW_VALUE
        cat edit.yaml

    
